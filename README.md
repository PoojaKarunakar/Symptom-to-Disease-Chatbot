# Symptom-to-Disease-Chatbot
A machine learning-based chatbot that predicts possible diseases based on user-provided symptoms. The system uses a Random Forest model to analyze symptom patterns and returns the top predicted diseases along with confidence scores. It also provides basic descriptions and precautions through an interactive Streamlit web interface.
